{
  "description": "Two-way order and shipment sync with ShipStation",
  "devDependencies": {
    "@swell/app-types": "^1.2.0",
    "typescript": "^5.9.3",
    "@cloudflare/vitest-pool-workers": "~0.12.0",
    "vitest": "5.0.x"
  },
  "name": "shipstation",
  "private": true,
  "scripts": {
    "typecheck": "([ -z \"$(find functions -name '*.ts' 2>/dev/null | head -1)\" ] || tsc --noEmit) && ([ ! -f test/tsconfig.json ] || tsc --build --noEmit test) && ([ ! -f frontend/tsconfig.json ] || tsc --build --noEmit frontend)",
    "test": "vitest run"
  },
  "version": "1.0.0",
  "workspaces": [
    "frontend"
  ]
}
